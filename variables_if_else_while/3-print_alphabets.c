#include <stdio.h>

/**
 * main-alphabet
 * Return: always 0
 */
int main(void)
{
	char c = 'a';

	while (c <= 'z')
	{
		putchar(c);
		c++;
	}
	putchar('\n');

	return (0);
}
{
	char d = 'A';
	while (d <= 'Z')
	{
		putchar(d);
		d++;
	}
	putchar ('\n');

	return (0);
}

